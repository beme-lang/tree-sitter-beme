{
  "name": "tree-sitter-beme",
  "type": "module",
  "version": "0.4.0",
  "description": "Tree-sitter grammar for the beme language",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/xpojure-lang/tree-sitter-beme.git"
  },
  "keywords": [
    "tree-sitter",
    "parser",
    "beme",
    "clojure"
  ],
  "main": "bindings/node",
  "types": "bindings/node",
  "scripts": {
    "build": "tree-sitter generate && node-gyp build",
    "test": "tree-sitter test",
    "parse": "tree-sitter parse"
  },
  "dependencies": {
    "node-gyp-build": "^4.8.0"
  },
  "peerDependencies": {
    "tree-sitter": "^0.22.0"
  },
  "peerDependenciesMeta": {
    "tree-sitter": {
      "optional": true
    }
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.26.7"
  }
}
